# 浙工大转专业机考真题

## 2023年5月 计算机 转专业 机考

见 `真题题解.md`（已整理 C 语言题解）

---

## 2024年12月 计算机科学与技术学院 转专业一志愿机试

> 来源：CSDN @JackLin_513
> 原文链接：https://blog.csdn.net/JackLin_513/article/details/144578193

### 题目列表

| 题号 | 题目 | 类型 |
|------|------|------|
| A | 最好的你 | 送分输出 |
| B | 判断坐标 | 条件判断 |
| C | 画字母M | 画图题 |
| D | 去除质数 | 数组操作 |
| E | 分析文章 | 字符串分析 |

---

### Problem A: 最好的你

**题目描述**
参加转专业测试的你在人群中一定是闪闪发光的那一个。请在屏幕上输出我对你的肯定和对你的祝福吧！

**输入**
无。

**输出**
按题目样本描述输出示范内容，行末回车。

**样例输入**
无。

**样例输出**
```
~ \You're the best!/ ~
=You will be succeed.=
```

**注意**
转义字符 `\` 需要写成 `\\`。

---

### Problem B: 判断坐标

**题目描述**
整数直角坐标系中的一个位置被描述为由横坐标和纵坐标构成的一对值，可以使用(x,y)这样的形式描述。
横纵坐标将直角坐标系分隔为7个不相交的部分，包括：第1象限，第2象限，第3象限，第4象限，
x轴，y轴和原点。请你根据给定的坐标值判断位于直角坐标系的哪个部分？

7个部分分别表示如下：
1. 第1象限: First quadrant
2. 第2象限: Second quadrant
3. 第3象限: Third quadrant
4. 第4象限: Forth quadrant
5. x轴: X axis
6. y轴: Y axis
7. 原点: Origin

**输入**
多组数据，每组数据一行，由一个坐标值构成，描述为(x,y)。
横纵坐标分别用逗号分隔，可能会使用 +/- 符号，无多余空格，使用圆括号标记为一组。
所有坐标值均不超过 int 范围。

**输出**
对应每组输入，输出一行描述对应坐标位于直角坐标系的哪个部分。行末直接回车。

**样例输入**
```
(3,+3)
(-1,0)
(0,-0)
```

**样例输出**
```
First quadrant
X axis
Origin
```

**C语言参考代码**
```c
#include <stdio.h>

int main(void) {
    int a, b;
    while (scanf("(%d,%d)", &a, &b) != EOF) {
        getchar();  // 吃掉换行符，否则会死循环
        if (a == 0 && b == 0) {
            printf("Origin\n");
        } else if (a == 0) {
            printf("Y axis\n");
        } else if (b == 0) {
            printf("X axis\n");
        } else if (a > 0 && b > 0) {
            printf("First quadrant\n");
        } else if (a < 0 && b > 0) {
            printf("Second quadrant\n");
        } else if (a < 0 && b < 0) {
            printf("Third quadrant\n");
        } else if (a > 0 && b < 0) {
            printf("Forth quadrant\n");
        }
    }
    return 0;
}
```

---

### Problem C: 画字母M

**题目描述**
小M想用计算机画自己的名字M，现在请你来帮达成这个心愿吧！

**输入**
多组数据输入，每组数据给出M的高度值 H（H ____，____）和构成的字符。

**输出**
输出由给定字符构成的字母 M 形状。

> 注：原文描述被截断，请参考 CSDN 原文获取完整题目描述和样例。

---

### Problem D: 去除质数

**题目描述**
（原文描述被截断，请参考 CSDN 原文）

---

### Problem E: 分析文章

**题目描述**
（原文描述被截断，请参考 CSDN 原文）

---

### 博主经验总结

> 1. 能用 C++ 就用 C++，STL 库可以节省大量时间
> 2. C 语言的格式化输入容易出错
> 3. CSDN 上的往年真题练完后，最好再去浙工大 ACM 平台练习，把通关考例题刷完
> 4. 机考共 5 道题，做出 3 题可以进入面试环节
> 5. 前三题通常不难，考试时不要着急
