#include <stdio.h>

#define PI 3.14     // 宏定义

int main(void) {

    const int apple_price = 100;
    
    printf("The price of apple is %d\n", apple_price);
    printf("PI: %lf\n", PI);

    return 0;
}